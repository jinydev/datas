---
layout: docs
title: "05. 열 참조"
---

## 05. 열 참조

다음 코드 `pf.loc[:, '열명']`으로 열 전체를 참조할 수 있다.

```python
pf.loc[:, '과제']
```

열도 슬라이싱이 가능하다.

```python
pf.loc[:, '중간':'과제']
```

열도 목록으로 여러 열을 참조할 수 있다.

```python
pf.loc[:, ['기말', '출석']]
```
